# Week 6 学习笔记 — RAG 基础流程

> 每晚看 20min + 写 30min，卡住也至少写两行，不断更。
> 本周卫星项目：基础 RAG Agent（可溯源 + 诚实拒答），检索能力后续并入主线「备忘录检索」（V3 D7）。
>
> **📌 发版日：09-17（周四）** ← 09-14 周一确认（加速计划 §〇.1 规则 1）。**发版当晚豁免一切学习与 recall**（不补练、不计入 D 编号）；若临时漂移改期，按 §〇.1「发版当晚豁免 + 内容弹性顺延」处理。

---

## ⚠️ 开工前必读：W5 已知陷阱清单（09-18 抽取）

> 来源：`week5/.env` / `week5/embed_demo.py` / `week5/retrieval_app.py` / `week5/build_index.py` / `week5/notes.md`。
> **这 5 条你 W5 全部踩过或亲手写过警告，而 W6 的脚手架已经复现了第 1、2、5 条。**
> 用法：**动手写码之前先过一遍。**
> 📌 抽它的理由：本轮的失败不是"笔记写得不够"，而是"**笔记里有答案，写码时没去查**"——
> 与 09-16 记的「笔记缺了推导链的一环」正好是两个方向、同一个根因。

| # | 陷阱 | 正确做法 | 出处 | W6 现状 |
|:--:|---|---|---|:--:|
| 1 | 用一个 client 同时打 chat 和 embedding | **拆两个 client**：embedding 走百炼 `text-embedding-v4`（用 `EMBEDDING_*`），chat 走 DeepSeek（用 `DEEPSEEK_*`）。DeepSeek **没有** `/embeddings` 端点（实测返回 **404**） | `week5/.env` L1-2／`embed_demo.py` L12 | ❌ **已复现**：`embed()` 复用 chat client + 写了个不存在的 `deepseek-embedding` |
| 2 | 用 `get_or_create_collection` | 用 **`get_collection`**：拿不到就抛异常。**失败要吵，不要假通过**——`get_or_create` 会静默建一个**空库**，让"找不到"长得像"成功" | `retrieval_app.py` L72／`notes.md` 09-16 知识点 #3 | ❌ **已复现**：`COLLECTION="week6_docs"` 在 W5 库里不存在 → 建空库 |
| 3 | 把 `distances` 当相似度 | `hnsw:space=cosine` 时 `distance = 1 - similarity`，**越小越像**；拒答判据是 `distance > 阈值` | `notes.md` 09-16 知识点 #1 | ✅ 骨架正确（已用 `1 - d` 换算，不必改） |
| 4 | 建 collection 时没声明 `cosine` | 显式 `metadata={"hnsw:space": "cosine"}` —— Chroma 默认 l2，且 **space 绑在 collection 上、建完改不了** | `build_index.py` L114-118 | ⚠️ 已复用 W5 的 cosine 库，本轮暂无风险 |
| 5 | 模块顶层依赖 cwd（`os.chdir()` 或相对路径） | 用 `BASE_DIR = os.path.dirname(os.path.abspath(__file__))` 拼**绝对路径** + `load_dotenv(路径)` | `retrieval_app.py` L41-43／`notes.md` 09-16 知识点 #4 | ❌ **已复现**：`PERSIST_DIR = "../week5/chroma_db"` + 裸 `load_dotenv()` → 只能在 `week6/` 目录下跑 |

---

## 一、本周开幕（20min，09-14 清账 D1）

> 依据：`每周开幕-全局定位.md` §三 W6 卡。**先自己写，再对答案**（§5.6：抄 = 未清偿）。

### ① 坐标一句话

> 格式：「本周 = 环 Lx 的 <作用>，治 Px，落 Dx 域，属层 Lx，是生命周期第 x 步」

**盲答初稿（09-14，未翻卡）**：环 L2｜治 P2｜落 D7｜属层 L2+L3｜第 2 步 → **6 空错 3.5**

**正解**：
> 本周 = 环 **L1 + L2** 的「为上下文组装准备高质量数据源 / 模型决策」，治 **P7**（质量决定决策）+ **P3**（决策是概率的 → RAG 压幻觉），落 **D2**（上下文供给）+ **D3**（决策可靠性）域，属层 **L3**（+ L4 苗头：30 条用例），是生命周期 **第 ④ 步收口**（+ 第 ⑥ 步评测闭环先声）。

⚠️ **踩坑记录（重要）**：盲答两轮均出现「**P↔D 配对自洽的错**」——第一轮 P2+D7（速查表 P2→D1/D7 自洽）、第二轮 P1+D1（P1→D1 自洽）。
→ 判据表被当成了**生成器**（先挑 P 再配 D），而不是**校验器**。**台账 #14 模式惯性复发 ×2**。

### ② 三行接口

```
上游（W5）给我什么：
本周我做什么：
交什么给下游（W7）：
```

**盲答初稿**：只说了 R（检索）+ A（拼上下文）→ **漏了 G（LLM 生成）**

⚠️ **这是台账 #3 的复发**：W5 周三口述测验已漏过一次 G，今晚原样再漏。W6 卡的上游接口段**专门点了这件事**（原文：「这是 W5 口述测验里你最大的洞（Q4 漏掉 RAG 的 G）」）——被开幕卡预言。

**正解**：
- 上游（W5）：一组语义相关的**【片段】**（不是答案）
- 本周：**R + A + G** —— 片段 → 拼进 prompt → **LLM 生成答案 + 附来源**
- 下游（W7）：一个**【能答、但不够准】**的 RAG 系统（W7 负责优化检索精度）

> 注意：W6 的产出是**答案**，不是「能处理的内容」。停在前者 = 只建了检索器，没建 RAG。

### ③ 生产之问（**自己手写**，3–5 行）

> 问：没有它，Agent 在生产里会怎么坏？坏了怎么发现？

**盲答初稿**：答成了「检索不到 → 循环卡死 → trace 里空 tool_use、token 烧不停」——**这是 W4 的死循环病（P4 / D4），不是 W6 的病**。

**正解**：检索不到时，真正的威胁是**模型拿训练知识硬答 = 幻觉**（属性 **P3**）。**这是 W6 存在的唯一理由。**

**拒答阈值的两难**（方向相反的两种事故）：

| 阈值 | 坏法 | 事故名 |
|:--:|---|---|
| **低** | 漏放 → 库里没有也编 → **答了不该答的** | **诚信事故** |
| **高** | 误拒 → 能答却说不会 → **不答能答的** | **可用性事故** |

→ **阈值不能拍脑袋**，要靠 **30 条用例的误拒 / 漏放分布**标定。**这就是 W6 必须建 30 条用例的真正理由**——不是为了打分，是为了标定拒答阈值。

**怎么发现**：用例通过率 + 人工抽查拒答样本。

> 🔗 **同源诊断**：坐标题漏 **P3** + 生产之问漏 **幻觉** = 同一个盲区——「**RAG 到底在压什么**」。
> W4 的死循环你记得牢（亲手实测 30 步撞线）；RAG 的威胁还没形成身体记忆，一被问就滑回 W4。

### ④ 三周上下文手画图

- [ ] 白纸画 `W5 → W6 → W7` 三框 + 接口箭头 → 拍照存 `week6/`（文件名：______）

---

## 二、评估趋势线（V3 D3，每周日更新）
| 周 | 用例数 | 通过率 | 本周改动 | 备注 |
|----|--------|--------|----------|------|
| W5 | 10 | __% | 检索基线 | |
| W6 | 30 | **66.7%** | 扩到 30 条+评分（正常 4/10 · 拒答 10/10 · 溯源 6/10） | ⚠️ 判据只验"链路通不通"、不验对错；10 条 FAIL 全为拒答形态 |

## 成本记录（V3，每周日更新）
| 周 | 模型 | 调用次数 | 花费 | 备注 |
|----|------|----------|------|------|
| W6 | deepseek-v4-flash | 60（30 embedding + 30 chat） | 未统计 | 09-19 首跑 30 条用例 |

## 面试题库（V3 D10，通勤每周攒 2 题）

> **攒题：狗子 ｜ 写答案：老张自己。** "必须覆盖"栏是**检查表**（有哪些点不能漏），不是答案成稿 ——
> 写的时候照它自查，别照抄。
> 📌 选题判据：优先挑**你有真实实测数字、别人只能背概念**的题 —— 这是你有项目、有生产环境的差异化。

### 题 1（RAG 流程 + 拒答设计）

**问**：完整讲一遍 RAG 的流程。跑通之后，你怎么保证它不拿训练知识乱答？

**必须覆盖**：
- **R → A → G 三步都要点到**（检索 / 拼上下文 / **LLM 生成**）
  ⚠️ 你在 W5 口述测验里漏过一次 G —— 这次别再停在前两步，停在前两步 = 只建了检索器、没建 RAG
- 检索不到时真正的威胁 = 模型拿训练知识硬答 = **幻觉**（属性 P3）
  ⚠️ **别答成"循环卡死 / token 烧不停"** —— 那是 W4 的死循环病（P4/D4），不是 RAG 的病
- **两道防线**：① 检索层（代码判，判据 = 相似度数字）② 生成层（模型判，判据 = prompt 指令）
- ② 对代码**不可见** → 所以必须上**结构化输出**（JSON 的 `found` 字段），否则代码不知道模型已经拒答了

**你的一手弹药**：「太阳」实测 0.3550，落在 **0.35~0.40 漏放带**，防线① 放行、全靠防线② 兜住

### 题 2（召回率定义 / 阈值取舍）

**问**：召回率怎么定义？把召回率往上提，会付出什么代价？

**必须覆盖**：
- 召回率 = 该找回来的找回来多少；用它换掉的是**精确率** —— 两者天然打架
- 落到 RAG 上就是**拒答阈值的两难**：
  - 阈值调**低** → 漏放↑ → **诚信事故**（库里没有也编 → 答了不该答的）
  - 阈值调**高** → 误拒↑ → **可用性事故**（能答的说不会）
- **不能拍脑袋定** → 靠测试集（30 条用例）的误拒 / 漏放分布**标定** —— 这才是要建 30 条用例的真正理由

**你的一手弹药**：量子力学 **0.2539**（掉在 0.35 左边，被防线① 拦）/ 太阳 **0.3550**（漏放带）/ 发版 **0.6415**（正常区）

### 备选池（下周通勤可补位）

- **题 3（🔴 优先补位）**：你让模型返回结构化结果，它没按格式返回，你的系统应该怎么办？
  → 要点：契约四步（定 schema / 声明必填 / 校验 / 违反时的行为）；
    **绝不允许"静默当成功"**；`found` 默认 `True` 是 **fail-open**（错），安全默认该往"拒绝"倒；
    解析失败时不能把 `raw` 原文当答案返回（**伪权威**：一段自由文本 + 3 条来源，比拒答更危险）
  → 这题能顺手把你 Java 的 Contract / 防御式编程经验带进来
- **题 4**：如何评估一个 RAG 系统的效果？没有标注数据怎么办？
  → 要点：检索指标（召回/精确）与生成指标（忠实度/幻觉率）分层评；人工抽查拒答样本；
    自建小测试集是性价比最高的起点

## 周一：RAG 全链路概念
- **【亲手画】RAG 流程图（用户提问 → 向量化 → 检索 Top-K → 拼 prompt → LLM 生成 → 附来源）：**

```
（在这里画）
```

- 为什么"先检索再生成"能减少幻觉：
- `rag.py` 最小链路跑通了吗（问库里有答案的问题，是否引用检索内容）：

### 【09-18 走查记录】rag.py 跑通

> 固定四栏，顺序不能反：**先写预测**（动手前）→ **现象原样抄**（一个字都别改）→ **差异** → **根因一句话 + 改了什么**。
> 顺序反了就等于事后编故事，预测的价值全丢了。

**第 1 轮**
- 预测（我猜会挂在哪一步、挂成什么样）：
- 现象（报错原文 / 输出原文）：
- 差异（预测 vs 实际的差在哪）：
- 根因（一句话）：
- 改动：

**第 2 轮**
- 预测：
- 现象：
- 差异：
- 根因：
- 改动：

**第 3 轮（跑通后）**
- 现象（问库里有的问题，答得对吗、带来源吗）：
- 结论：

**待办：**
- [ ] RAG 流程图拍照存 `week6/`（文件名：__________）


## 周二：拼上下文 + prompt 设计
- system prompt 怎么把检索结果拼进去（`<context>...</context>` 三段式）：
- 拼法 A（原文直接拼）vs B（加"上下文没有就说不知道"指令）对比结论：
  →

## 周三：诚实拒答 + 溯源
- 拒答机制：检索相关度过低 → 诚实说"不知道"
- 溯源：来源 metadata（文件名/标题/段落）怎么回显
- 阈值定多少？测"库里没有的话题"是否诚实拒答：
  →

### 【09-19 拒答机制消融实验】把"两道防线"拆开看

> **目的**：弄清"太阳"（相似度 **0.3550**，卡在 0.35 阈值**上方**、落在漏放带）究竟是被哪一道拦住的。
> **做法**：临时摘掉 ② 生成层的三处约束 ——（a）角色约束「严格基于资料的助手…只根据资料回答」→「你是一个助手」；（b）`found=false` 的字段说明；（c）第二道防线的代码判定 `if not found or not answer`；并加 `print(answer)` 直读模型原文。

**现象（原样抄）**：

```
问> 太阳
资料中没有关于「太阳」的内容，无法回答。现有备忘主要涉及护眼、久坐对策、饮食、睡眠...
来源> doc_039.md, doc_053.md, doc_087.md
```

**三点观察**：

| # | 观察 | 含义 |
|:--:|---|---|
| 1 | 摘掉角色约束后，模型**仍然拒答**（只是换了措辞，不再是固定文案） | 拒答是**模型对「资料与问题不匹配」的判断**，不是 prompt 顺从。防御有冗余 —— 但也意味着**不能拿"它拒答了"当成"prompt 写得好"的证据** |
| 2 | 但多了一条尾巴：「现有备忘主要涉及护眼、久坐对策、饮食、睡眠…」 | **不是幻觉**（确实来自资料），是**越权**：回了一句用户没问的。危险在演化方向 —— 资料里若恰好有沾"太阳"边的边角料，就会从「越权」滑成「漏放」 |
| 3 | 代码侧来源照打 3 条 | 第二道防线在代码里是**隐形**的：不上 JSON，代码根本不知道模型已经拒答了 |

**结论**：角色约束界定的**不是能力，是输出边界** —— 它不是"让模型会拒答"，是"让模型**只**拒答"。而 ② 生成层是 0.35~0.40 **漏放带**（如"太阳"）的**唯一**拦截者，所以它必须对代码可见 —— **这才是 JSON 结构化输出的真正理由**。

**回滚**：实验后已还原 5 处 —— 角色约束 / 字段说明 / 第二道防线 / 去掉调试 `print` / 注释里 `json_format` 正名为 `response_format`。

## 周五：测试用例扩到 30 条 + 评分 + Java 对照
- 30 条用例分类：正常问答 10 + 拒答场景 10 + 溯源要求 10
- `python test_cases.py` 通过率：**66.7%（20/30）** —— 目标 ≥85% 未达，如实记录
- Java 对照输出物：`week6/java/` 下 Spring AI `QuestionAnswerAdvisor` RAG 片段
- 对照笔记（Python 手写 RAG ↔ Spring AI QuestionAnswerAdvisor）：
  →

### 【09-19 首跑 30 条用例】结果与「判据有效性」问题

**命令**：复用 `week5/.venv` 跑 `test_cases.py`（包了一层明细输出，逐条打印 问/答/来源）。
**总通过率：20/30 = 66.7%**

| 类 | 通过 | 失败形态 |
|:--:|:--:|---|
| 正常问答（#1–10） | **4/10** | 6 条**全部**是拒答（`我不知道（资料中没有相关信息）。` + `SRC: []`） |
| 拒答（#11–20） | **10/10** | — |
| 溯源（#21–30） | **6/10** | 4 条**全部**是拒答 + 空来源 |

**四条事实观察**：
1. **10 条 FAIL 是同一种形态**：全部拒答 + 空来源，**没有一条是"答错"** → 失败点不在生成层，在检索/判断层。
2. **拒答类 10/10 全过，但没考到防线②**：「量子力学」实测 0.2539 → 被**防线①** 拦。这 10 条全落在阈值下方，
   防线② 需要**漏放带（0.35~0.40）**的题才上场（例：「太阳」0.3550）→ **这 10 条没覆盖防线②**。
3. **PASS 里也放水**（印证判据洞 1）：#9「最近要交什么」答的是「发票必须当月交」—— 是"**怎么交**"不是"**交什么**"，
   **答偏了但 PASS**。所以 66.7% 里含水分。
4. **top-k 出现重复文档**：#29「体检记录」来源 `['doc_047.md','doc_003.md','doc_047.md']` —— `doc_047` 出现两次（不同 chunk）。
   不是 bug，但暴露 **top-k 缺多样性** → rerank / MMR 的发力点。

**⚠️ 核心问题：10 条 FAIL 分不出两类** ——
- **A 类**：资料里**真的没有** → 拒答是**正确**行为，却被记为 FAIL（**用例期望值设错了**）
- **B 类**：资料里**有**但检索打不中 → 拒答是**错误**行为（真失败，W7 要治）

两类**处方相反**：A 改期望值、B 上 rerank。**在分出来之前，66.7% 不能当作 W7 的优化基线。**
→ **验证结果见下节：B 类实测 = 0 条**（结论已修正，别停在上面这行）。

**下一步验证法**：把这 10 条 FAIL 的 query 做**无阈值检索**（不做拦截，直接打 top-3 + 相似度），人工判断资料在不在库里。
⚠️ 动手前**先写预测**：哪几条 A 类、哪几条 B 类。

### 【09-19 FAIL 归因验证】10 条 FAIL 到底断在哪

**老张预测**：A 类 6 条（#1 #5 #4 #21 #25 #30）／B 类 4 条（#2 #6 #10 #28）。

**方法**：① 无阈值语义检索（打 top-3 + 相似度）② 词典命中（`where_document={"$contains": 词}`）
③ 语料主题清单。**健全性检查已过** —— `$contains` 对 `主题`(100) / `采购`(15) / `变更`(39) / `体检`(14) 均正常命中。

| # | query | maxsim | 实际拦在哪 | 语料里有吗 | 归因 | 预测 |
|:--:|---|:--:|---|---|---|:--:|
| 1 | 我的购物清单里有什么 | 0.5088 | **② 生成层** | ✅ 生活采买(15篇)/日用品补货 | **C 类** | A ❌ |
| 5 | 买了哪些食物 | 0.4779 | **② 生成层** | ✅ 生活采买/生鲜采购(肉类蔬菜) | **C 类** | A ❌ |
| 4 | 喜欢什么样的回答风格 | 0.3060 | ① 检索层 | ❌ 无此内容 | A 类 | A ✅ |
| 21 | 牛奶在哪个文档 | 0.3877 | **② 生成层** | ❌ 无"牛奶" | A 类 | A ✅ |
| 25 | 机票在哪 | 0.5669 | **② 生成层** | ❌ 无"机票"（但有出行旅行 18 篇） | A 类 | A ✅ |
| 30 | 买水果的记录 | 0.5345 | **② 生成层** | ❌ 无"水果"（生鲜采购只提肉类/蔬菜） | A 类 | A ✅ |
| 2 | 常用命令有哪些 | 0.4611 | **② 生成层** | ❌ 无"命令"（编程运维讲幂等/分支/日志） | A 类 | B ❌ |
| 6 | 会用什么编程工具 | 0.3703 | **② 生成层** | ❌ 无"工具" | A 类 | B ❌ |
| 10 | 个人偏好是什么 | 0.3420 | ① 检索层 | ❌ 无"偏好" | A 类 | B ❌ |
| 28 | 关于复习的安排 | 0.5257 | **② 生成层** | ❌ 无"复习"（日程是周会/健身/评审） | A 类 | B ❌ |

**语料主题清单（200 篇）**：发布值班 20 ／ 出行旅行 18 ／ 健康作息 17 ／ 日程 16 ／ 生活采买 15 ／ 编程运维 14 ／
（无主题标记 100 —— 是 chunk 切分后的后半段）。
→ 10 个查询关键词（购物/食物/牛奶/机票/水果/命令/工具/偏好/复习）**词频全部为 0**。

**三条硬结论**：
1. **8/10 的 FAIL 是被 ② 生成层拦的**（maxsim 0.37~0.57，全部 > 0.35）→ **不是检索不到，是检索到了、模型不肯答。**
2. **B 类（资料有但检索没打中）= 0 条。** 这 10 条里**没有一条**的失败原因是"检索精度不足"。
   8 条是语料里**根本没有那项内容**（用例期望值设错），2 条是**检索打中但生成层过保守**。
3. **高相似度 ≠ 相关**：#25 机票 → 检索到「酒店选择」sim 0.5669；#28 复习 → 检索到「健身房安排」sim 0.5257。
   语义相近、内容不对口 —— 这是 bi-encoder 粗筛的固有局限，也是 rerank/cross-encoder 的经典动机；
   **但前提是"库里得有对的那篇"**，而这批题里没有。

**⚠️ 对 W7 计划的直接影响**：W7 原定「上 rerank 提检索精度」。
按本批数据，**rerank 修不了这 10 条中的任何一条**（没有一条是检索漏召回）。
当前真实病灶排序：**① 用例期望值错（8 条）＞ ② 生成层过保守（2 条）＞ ③ 检索精度（0 条证据）**。

**预测复盘**：命中 **4/10**（A 类 4/6，**B 类 0/4 全错**）。
偏差方向一致 —— 他把「主题沾边」当成了「资料里有」（#2 命令↔编程运维 / #6 工具↔编程运维 / #28 复习↔日程）。
→ **校准点：A/B 的判据不是"主题像不像"，是"库里有没有能回答这条问题的具体内容"。**

### 【09-19 rerank 可测性诊断】测试集缺什么才能真正验证 rerank

> 触发：老张指出「数据不支持 rerank 提精度 → **测试集里需要有能支持 rerank 的验证内容**」。

**诊断 A · 相似度聚集**（20 条"应该有答案"的题，各取 top-10 相似度）

| 指标 | 实测 |
|---|---|
| rank1 − rank10 落差 | 0.020 ~ 0.116（多数 < 0.05） |
| **rank3 − rank4 落差** | **13/20 条 < 0.005** |

→ embedding 给出的排序**几乎是平的**：第 1 名与第 10 名只差零点零几，相邻两名常只差 0.001。

**诊断 B · 语料重复**（⚠️ 第一版判错，已修正）

- 文档级：两两余弦 >0.95 仅 **11 对 / 18 篇**；>0.90 有 120 对 / 92 篇。
- **但段落级重复极其普遍** —— 同一段内容被复制进多篇：
  - 「大促囤货」top1 `doc_064` / top2 `doc_022` / top5 `doc_085` **是同一段"日用品补货"**
  - 「变更评审」top1 `doc_063` / top2 `doc_100` / top3 `doc_014` / top4 `doc_066` **是同一段"变更评审"**
- → **结论修正**：语料是**一批段落被随机组合成 200 篇** → 同一内容有多个副本。
  "相似度平"的主因**不是 embedding 分辨力差，是候选内容实质相同**（给相同内容打相同分是**正确**行为）。
- ⚠️ **教训：doc 级相似度阈值测不出"段落级复用"** —— 评估语料质量必须下钻到 chunk 级。

**诊断 C · 排序倒挂真实存在（好消息）** —— 实测捞到 2 个货真价实的 rerank 样例：

| query | embedding 的 top1（不对口） | 正解（被压到第 3） | 落差 |
|---|---|---|:--:|
| 报销单怎么提交 | `doc_048`「**机场值机**」0.4868 | `doc_028`「**出差报销**」0.4529 | 0.034 |
| 蔬菜和肉什么时候买 买多少 | `doc_016`「快递售后…」0.6297 | `doc_087`「**生鲜采购**」0.5807 | 0.049 |

→ **rerank 有真实用武之地，前提是"正解唯一、且被压在后面"。**

**诊断 D · 附带发现：语料组织方式会污染判据**
每篇文档 = 头部 + **多段拼接** → 截断看前 100 字会**误判**"这篇到底讲不讲这个"。
且「正解」往往不是一篇，而是**同一段内容的多个副本构成的集合**。
→ 所以 `expect_source` 不能填单篇，要填**一组**（或用"正解段落的关键句"做匹配）。

**→ W7 造题的落地方案（三条判据 + 一条方法）**
1. **标正解**：`expect_source` = 一组文档（同一段内容的副本集合）
2. **正解在池中** —— 排除 A 类"库里根本没有"（那 8 条再强化判据也测不出排序）
3. **正解必须被 ≥1 篇"像但不对口"的文档压在后面** ← **rerank 唯一能翻盘的场子**
4. **方法**：对语料跑一遍无阈值检索，凡出现「top1 不对口、正解在 rank 2–5」的 query，就是**天然的 rerank 样例**
   （本次已捞到 2 条：「报销单怎么提交」「蔬菜和肉什么时候买 买多少」）

## 周末项目：基础 RAG Agent（卫星）
- 验收 1（有答案→带来源）/ 2（无答案→拒答）/ 3（30 条用例通过率）结果：
- README 一句话卖点「可溯源 RAG + 诚实拒答」写了吗？
- 空白页测验结果（重写检索→拼上下文→生成核心）：
- 与主线衔接：「备忘录检索」功能需求写清楚了吗（W7 并入用）：

## 本周复盘（3 句话）
- 收获：
  1. **两道拒答防线**：**检索层**（代码判，判据 = 相似度数字）vs **生成层**（模型判，判据 = prompt 指令）。
     关键发现：**生成层对代码不可见** —— 不上结构化输出（JSON 的 `found` 字段），代码根本不知道模型已经拒答了。
  2. **阈值是硬边界，边界带（0.35~0.40）是系统性漏放区** ——「太阳」0.3550 实测落在里面。
     我原本以为输入「太阳」会**直接触发拒答**（因为知识库里没有太阳的内容），实测是：**防线① 放行、靠防线② 兜住的**。
  3. 由此确认：**W6 相似度阈值的核心是「按测试集来配置」**，不是拍脑袋定的。
- 卡点：`rag()` 这类整合调用链路的函数，我缺的不是语法、也不是"见得少"——是**跨语域的模式映射**（模式库有货，但没建索引：Java 中台的熔断 / 降级 / 重试 / Contract，没被认出来就是 Agent 编排里的阈值拒答 / `REFUSE_TEXT` / 重试 / JSON schema）。
  具体表现：对 Agent 执行链路的**降级策略**（何时、在什么位置、如何设置）没有意识；**契约约定**没有转换成工程概念与工程自觉。
  → **转化动作**：把"直觉"换成"清单"——清单 = 规范，反复过 → 长成习惯。清单见 [`Agent编排清单.md`](../Agent编排清单.md)，W7 逐条落地。
- 下周重点（W7 RAG 优化 + Java 重写主线）：
  1. **先建尺子，再上 rerank**：补 **5–10 条「带正解标注 + 含 hard negative 竞争」的用例**，建立「**正解排名**」这个度量，
     再拿它去评 rerank。
     - 三条判据：① 标 `expect_source`（⚠️ 正解往往是**一组副本**而非一篇）② 正解在池中
       ③ **正解被 ≥1 篇"像但不对口"的文档压在 rank 2–5** ← rerank 唯一能翻盘的场子
     - 捡题方法：对语料跑**无阈值检索**，凡出现「top1 不对口、正解落 rank 2–5」的 query 即为天然样例（09-19 已捞到 2 条）
     - ⚠️ **没有这把尺子，rerank 的收益不可测** —— 详见上方「rerank 可测性诊断」
  2. **按清单补 `rag.py`**：拿 [`Agent编排清单.md`](../Agent编排清单.md) 的**降级 6 条 + 契约 4 步**逐条过一遍并落地
     （含今天照出的两个洞：`found` 默认 `True` 的 fail-open、解析失败把 `raw` 当答案返回的泄漏）。
  3. **把所有要做的事都罗列成明确清单**，再照清单一条条做，靠反复执行把清单变成工程习惯。
  4. **Java / Spring AI 重写主线**：前置动作 —— 先把 Spring AI 环境完整跑起来一次（`week6/java/`）。
     判断：**Spring AI 是面试里最重要的部分**。

---

## 【09-19 晚间】契约收口：从清单到代码

> **性质**：`Agent编排清单.md` 建档后**第一次真正落地**。清单第 2 项（W7 逐条补 `rag.py`）里
> 契约部分的 2 处提前做了。**代码由老张手写，狗子只给靶子和验证。**

### 一、改动清单（全部实测）

| # | 位置 | 改前 | 改后 | 为什么 |
|:--:|---|---|---|---|
| 1 | `found` 默认值 | `bool(data.get("found", True))` | `bool(data.get("found", False))` | **安全默认必须往「拒绝」倒**。默认 `True` = 模型少给字段时被当成「有答案」放行 → **防线② 被静默绕过** → 幻觉（**诚信事故**） |
| 2 | 异常分支返回值 | `return (raw or REFUSE_TEXT), metas` | `return REFUSE_TEXT, []` | **伪权威**：把模型自由文本当答案 + 带 3 条看起来权威的来源，比诚实拒答更危险。来源用 `[]` 不用 `metas` |
| 3 | 异常分支留痕 | 无 | `print("json解析报错, 大模型返回为:", raw, file=sys.stderr)` | 让违约**可观测**（清单 §一 #6 **首次落地**）。用 stderr：**日志给开发看、不给用户看**（`main()` 是交互式循环，stdout 混日志 = 内部实现泄漏） |
| 4 | **字段缺失留痕**（新增） | 无 | `if "found" not in data or "answer" not in data:` → stderr + `return REFUSE_TEXT, []` | **JSON 合法但字段缺失**不抛异常 → 走 `try` 正常结束 → `except` **永远抓不到**。必须自己判 |
| 5 | 顺手修正 | `f"son解析报错"`（漏字母 + 无用 f）、`",大模型返回为 ".join(raw)`（`join` 误用 → 逐字符拼接） | 直接传 `raw` | 日志错字**不报错、长期潜伏**（台账 #16 同族）；`join` 误用是「能跑但产出垃圾」的静默 bug |

### 二、踩坑记录（三个，都是「不报错的错」或「改错地方」）

1. **删掉 `except` 里的 `return` → 运行期崩**
   把「L112 `if not found or not answer` 能兜住」的**逻辑**当成了「代码能走到那里」的**执行流**。
   实测崩溃：`UnboundLocalError: cannot access local variable 'found'`
   （`try` 里 `json.loads` 一抛异常，`found` / `answer` 从未被赋值，`except` 里又不 `return` → 带着未赋值变量往下走）。
   → **`except` 里不 `return` / 不 `raise` = 假装没出事继续跑。**
   → 教训：**「逻辑上能兜住」≠「执行上能走到」**。读代码看数据流，排障看控制流。
2. **改对了语义、丢了语法**
   `if "found" not in data or "answer" not in data` 少冒号 + 缩进 3 空格（对不上 12 空格层）→ 文件**编译不过**。
   → 建立动作：**落笔后先过语法编译**（零成本、不用 API）：
   `python -c "compile(open('rag.py',encoding='utf-8').read(),'rag.py','exec')"`
3. **「改了但没落盘」**
   22:10 声称改完，实测 `rag.py` mtime 停在 22:10:22、sha256 未变 → 编辑器缓冲没存盘。
   → 建立动作：**核 hash 再判定「已改」**，不看叙述看事实。

### 三、回归 6 条（mock 版，零 API 成本）

| # | 输入 `raw` | 返回 | stderr | 判定 |
|:--:|---|---|---|:--:|
| 1 | `{"found": true, "answer": "这是答案"}` | `(答案, [metas])` | 空 | ✅ 放行 |
| 2 | `{"found": false, ...}` | `(REFUSE_TEXT, [])` | 空 | ✅ 正常拒答 |
| 3 | `{"answer": "我编的答案"}`（**少 found**） | `(REFUSE_TEXT, [])` | `契约字段缺失, ...` | ✅ **关住 + 留痕** |
| 4 | `抱歉，我无法回答`（**JSON 坏**，改前会崩） | `(REFUSE_TEXT, [])` | `json解析报错, ...` | ✅ **不崩 + 留痕** |
| 5 | `{"found": true, "answer": ""}` | `(REFUSE_TEXT, [])` | **空** | ⚠️ 被防线② 兜住，但**无痕迹**（见下） |
| 6 | `{}` | `(REFUSE_TEXT, [])` | `契约字段缺失, ...` | ✅ 留痕 |

**stdout 全空** —— 用户信道干净，日志信道独立。

### 四、本次改动的边界（我知道自己没管什么）

- **分支 5 是第三种「无痕迹违约」**：`found=true` 但 `answer` 空 —— 契约上 `found=true` 的**义务**是给出正文，
  值不合义务；但当前检查**只看键在不在、不看值合不合法**。
  → 清单 §二 步骤 3「校验」的下一层：**存在性 → 类型 / 值域**。**留 W7。**
- **仍未做**：降级 6 条里 **#1 超时 / #2 重试退避 / #3 回退次级路径**。
  `retrieve()` 里 `embed()` 会挂住且**无超时**（藏在另一层，从 `rag()` 看不出来）；`col` 空值未防
  —— 见 `rag.py` L45 / L46 自己写的批注。

### 五、清单落地情况（更新）

| 清单项 | 改前 | 改后 |
|---|:--:|:--:|
| §一 #4 拒答 | ✅ | ✅ |
| §一 #5 返回默认值 | ✅ | ✅ |
| **§一 #6 落库待查** | ❌ | **✅（stderr 留痕首次落地）** |
| §一 #1 超时 / #2 重试 / #3 回退 | ❌ | ❌（W7） |
| §二 #2 声明必填 | ⚠️ fail-open | **✅** |
| §二 #3 校验 | ⚠️ 仅 `json.loads` | **✅ 存在性检查**（值域校验留 W7） |
| §二 #4 违反时的行为 | ⚠️ 泄漏 `raw` | **✅** |

### 六、睡前 3 问（口述，全部通过）

| 问 | 老张答案 | 判 |
|---|---|:--:|
| 键缺失 / JSON 坏，分别走哪两个分支？ | **L99 / L104**（行号，实测核对正确） | ✅ |
| 为什么 `found` 默认必须 `False`？ | 「不能给出大模型有幻觉的答案，宁愿说不知道 —— **诚信的回答是最重要的**」 | ✅ |
| 日志为什么不能放 stdout？ | 「**日志是给开发检查代码看的，而不是给用户看的**」 | ✅ |

---

### 📌 当晚诊断（狗子的观察，供 W7 复盘用）

**同一个形状，一晚出现 4 次**：

| # | 场景 | 他答对了什么 | 他漏了什么 |
|:--:|---|---|---|
| 1 | `embed()` | 作用（算向量） | **位置**（排第几） |
| 2 | `retrieve()` 的 API | 防御清单（全对） | **位置** |
| 3 | 删 `return` 后 | 逻辑（防线② 能兜） | **执行流**（变量未赋值 → 崩） |
| 4 | 修字段检查 | 语义（引号加对了） | **可运行性**（少冒号 + 缩进错） |

**共同点**：每次都在「某一层」答对，**没往下验证一层**。而验证一层的成本极低
（L99/L104 是位置、compile 是语法、hash 是落盘）。

→ **对照组（重要）**：同晚也有答得完整的三条 ——
「为什么 `False`」（因果链四步齐）、「日志为什么 stderr」（答到「给谁看」）、
「`test_cases.py` 会受影响吗」（控制流判断）。
**这三条都不是「记住的」，是「推出来的」** —— 说明缺的不是能力，是
**「答完就停、不再推一层」的习惯**。

---
